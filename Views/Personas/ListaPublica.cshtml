@model IEnumerable<Escuela_Sor_Maria.Models.tbPersona>


@{
    ViewData["Title"] = "ListaPublica";
}

<div class="container">
    <h2>BienVenido</h2>
    <p> Te presentamos la lista de profesores de nuestra institución , a ca podras ver la asignatura que imparte el docente asi como su numero de contacto</p>
    <hr />
    <div class="shadow-mj br-15 px-1 py-3 offset-12">
        <div class="table-responsive">
            <table class="table table-striped">
                <thead>
                    <tr class="text-white">
                        <th>
                            @Html.DisplayNameFor(model => model.Nombre)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Apellido1)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Apellido2)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Sexo)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Telefono)
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr class="text-white">
                            <td>
                                @Html.DisplayFor(modelItem => item.Nombre)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Apellido1)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Apellido2)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Sexo)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Telefono)
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

